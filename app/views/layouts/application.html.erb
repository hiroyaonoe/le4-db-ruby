<!DOCTYPE html>
<html>
  <head>
    <title>Le4DbRuby</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
      ユーザー: <%= current_user.name %> <%= button_to "ログアウト", destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to "ログイン", new_user_session_path %>・<%= link_to "アカウント作成", new_user_registration_path %>
    <% end %>

    <%= yield %>

    <%= link_to "ホームへ戻る", root_path%>
  </body>
</html>
